package SVG_Tools.JunkClasses.SVG_Elements;

import SVG_Tools.JunkClasses.SVG_Elements.SVG_Parameters.Circle_Parameter;
import SVG_Tools.JunkClasses.SVG_Elements.SVG_Parameters.IF_SVG_Parameter;

public class Circle_Element extends SVG_Element
{
    public Circle_Element(Circle_Parameter element) {
        if (element instanceof IF_SVG_Parameter)
        {

        }
    }
}
